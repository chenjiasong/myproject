spring:
  cloud:
    config:
      name: myproject-consumer-dept #需要从github上读取的资源名称，注意没有yml后缀名
      #profile配置是什么就取什么配置dev or test
      label: master
      uri: http://localhost:3344  #SpringCloudConfig获取的服务地址

