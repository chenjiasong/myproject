spring: 
  cloud: 
    config: 
      name: myproject-eureka           #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev                    #需要读取哪个配置
      label: master                   #github上的版本
      uri: http://localhost:3344      #SpringCloudConfig获取的服务地址

